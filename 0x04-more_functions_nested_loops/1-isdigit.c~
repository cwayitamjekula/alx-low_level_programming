#include "main.h"

/**
 * _isdigit - determine if input is a number
 *
 * @c: character
 * Return: 1 if digit, otherwise 0
 */

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
return (1);
else
return (0);
}
